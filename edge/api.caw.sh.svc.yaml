---
apiVersion: v1
kind: Service
metadata:
  name: api-caw-sh-svc
  namespace: apis
  labels:
    app: novelcovid
    cluster: apis.novelcovid
spec:
  ports:
    - name: http
      port: 3000
      protocol: TCP
      targetPort: 3000
  selector:
    app: novelcovid
    cluster: apis.novelcovid
  type: ClusterIP
