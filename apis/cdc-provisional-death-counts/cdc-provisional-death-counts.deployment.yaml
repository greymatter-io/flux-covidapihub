---
apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: cdc-provisional-death-counts
    cluster: apis.cdc-provisional-death-counts
  name: cdc-provisional-death-counts
  namespace: apis
spec:
  replicas: 1
  selector:
    matchLabels:
      app: cdc-provisional-death-counts
      cluster: apis.cdc-provisional-death-counts
  template:
    metadata:
      labels:
        app: cdc-provisional-death-counts
        cluster: apis.cdc-provisional-death-counts
    spec:
      serviceAccountName: apis-sa
      containers:
        - name: cdc-provisional-death-counts-proxy
          image: "docker-dev.production.deciphernow.com/deciphernow/gm-proxy:1.4.1-dev"
          imagePullPolicy: IfNotPresent
          args:
            - -c
            - /etc/greymatter/config.yaml
          command:
            - /app/gm-proxy
          ports:
            - name: metrics
              containerPort: 8080
            - name: sidecar
              containerPort: 8443
          volumeMounts:
            - name: sidecar-config
              mountPath: /etc/greymatter
              readOnly: true
            - name: spire-socket
              mountPath: /run/spire/sockets
              readOnly: false
      imagePullSecrets:
        - name: docker-dev.production.deciphernow.com
      volumes:
        - name: spire-socket
          hostPath:
            path: /run/spire/sockets
            type: DirectoryOrCreate
        - name: sidecar-config
          configMap:
            name: cdc-provisional-death-counts-sidecar
